import React from'react';import{CheckCircle2,Clock,FileCheck2}from'lucide-react';import PageHeader from'../../components/common/PageHeader';import{evidence}from'../../data/mockData';export default function AdminVerification(){return <><PageHeader eyebrow="ADMINISTRATION" title="Verification Queue" description="Review synthetic evidence records before they enter the prototype verified state."/><div className="cards-grid">{evidence.filter(e=>e.status!=='Verified').slice(0,12).map(e=><div className="card panel" key={e.id}><div style={{display:'flex',justifyContent:'space-between'}}><FileCheck2 size={18} color="#72bdff"/><Clock size={14} color="var(--warning)"/></div><h3 style={{font:'600 12px Manrope'}}>{e.title}</h3><p className="section-sub">{e.source} • {e.district}</p><button className="btn success" onClick={()=>alert(e.id+' marked for verification.')}><CheckCircle2 size={13}/> Review evidence</button></div>)}</div></>}
