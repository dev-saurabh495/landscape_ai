import React,{useState}from'react';import{Search,UserX}from'lucide-react';import PageHeader from'../../components/common/PageHeader';import{users}from'../../data/mockData';import StatusBadge from'../../components/common/StatusBadge';export default function AdminUsers(){const[q,setQ]=useState('');const list=users.filter(x=>(x.name+x.role+x.organization).toLowerCase().includes(q.toLowerCase()));return <><PageHeader eyebrow="ADMINISTRATION" title="User Management" description="RBAC-aware prototype user directory."/><div className="table-toolbar card"><div className="search-input"><Search size={15}/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search users…"/></div></div><div className="table-card card"><table className="data-table"><thead><tr><th>User</th><th>Role</th><th>Organization</th><th>Status</th><th>Last Active</th><th>Action</th></tr></thead><tbody>{list.map(u=><tr key={u.id}><td>{u.name}</td><td>{u.role}</td><td>{u.organization}</td><td><StatusBadge tone={u.status==='Active'?'verified':'warning'}>{u.status}</StatusBadge></td><td>{u.last}</td><td><button className="btn" onClick={()=>alert('User action simulated for '+u.name)}><UserX size={12}/> Manage</button></td></tr>)}</tbody></table></div></>}
