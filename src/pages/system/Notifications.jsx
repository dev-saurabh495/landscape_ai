import React,{useState}from'react';import{Bell,CheckCheck}from'lucide-react';import PageHeader from'../../components/common/PageHeader';import{notifications}from'../../data/mockData';export default function Notifications(){const[n,setN]=useState(notifications.map(x=>({...x,read:false})));return <><PageHeader eyebrow="SYSTEM" title="Notifications" description="Evidence, dispute, policy and system activity notifications." actions={<button className="btn" onClick={()=>setN(n.map(x=>({...x,read:true})))}><CheckCheck size={14}/> Mark all read</button>}/><div className="card panel">{n.map(x=><div key={x.id} style={{display:'flex',gap:12,padding:'13px 0',borderBottom:'1px solid rgba(29,58,85,.5)',opacity:x.read?.6:1}}><div className="kpi-icon"><Bell size={15}/></div><div style={{flex:1}}><b style={{fontSize:11}}>{x.title}</b><p style={{fontSize:10,color:'var(--muted)',margin:'4px 0'}}>{x.text}</p><small style={{fontSize:8,color:'var(--muted2)'}}>{x.time}</small></div>{!x.read&&<button className="btn" onClick={()=>setN(n.map(y=>y.id===x.id?{...y,read:true}:y))}>Read</button>}</div>)}</div></>}
