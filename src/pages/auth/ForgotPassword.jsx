import React,{useState}from'react';import{Link}from'react-router-dom';export default function ForgotPassword(){const[e,setE]=useState('');const[s,setS]=useState(false);return <div className="auth-card"><span className="eyebrow">ACCOUNT RECOVERY</span><h2>Reset access</h2><p>Enter your email to simulate a recovery request.</p>{s?<div className="card panel"><div className="status verified">Recovery request prepared</div><p style={{color:'var(--muted)',fontSize:11}}>Demo mode does not send an email.</p></div>:<form className="auth-form" onSubmit={x=>{x.preventDefault();setS(true)}}><div className="field"><label>Email address</label><input required type="email" value={e} onChange={x=>setE(x.target.value)} placeholder="demo@landevidence.ai"/></div><button className="btn primary auth-submit">Continue</button></form>}<div className="auth-footer"><Link to="/login">Back to sign in</Link></div></div>}
