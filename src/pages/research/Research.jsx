import React,{useState}from'react';import{BookOpen,Plus,Search}from'lucide-react';import PageHeader from'../../components/common/PageHeader';import{research}from'../../data/mockData';import StatusBadge from'../../components/common/StatusBadge';import DecisionModal from'../../components/common/DecisionModal';export default function Research(){const[q,setQ]=useState('');const[m,setM]=useState(false);const list=research.filter(x=>(x.title+x.district).toLowerCase().includes(q.toLowerCase()));return <><PageHeader eyebrow="RESEARCH WORKSPACE" title="Research Workspace" description="Organize evidence collections, datasets, notes, citations and policy studies." actions={<button className="btn primary" onClick={()=>setM(true)}><Plus size={14}/> New Research Project</button>}/><div className="table-toolbar card" style={{marginBottom:12}}><div className="search-input"><Search size={15}/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search research projects…"/></div></div><div className="cards-grid">{list.map(r=><div className="card report-card" key={r.id}><BookOpen size={18} color="#72bdff"/><h3>{r.title}</h3><p>{r.district} • {r.sources} evidence sources</p><StatusBadge tone={r.status==='Active'?'verified':'info'}>{r.status}</StatusBadge><div style={{marginTop:12}}><button className="btn">Open workspace →</button></div></div>)}</div>{m&&<DecisionModal title="New research project" text="Prototype project form opened conceptually. In this demo, the workspace is represented with static research collections." close={()=>setM(false)}/>}</>}
