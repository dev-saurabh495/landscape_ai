import React from'react';import{ArrowLeft,FileCheck2,ShieldCheck}from'lucide-react';import{Link,useParams}from'react-router-dom';import PageHeader from'../../components/common/PageHeader';import{evidence}from'../../data/mockData';import DecisionTrace from'../../components/common/DecisionTrace';export default function EvidenceDetail(){const{id}=useParams();const e=evidence.find(x=>x.id===id)||evidence[0];return <><PageHeader eyebrow="EVIDENCE RECORD" title={e.title} description={`${e.source} • ${e.district} • ${e.date}`} actions={<Link className="btn" to="/evidence"><ArrowLeft size={14}/> Back</Link>}/><div className="dashboard-grid"><div className="grid"><div className="card panel"><div className="metric-grid"><div className="metric-box"><span>Status</span><b style={{fontSize:12,color:'var(--success)'}}>Verified</b></div><div className="metric-box"><span>Confidence</span><b>{e.confidence}%</b></div><div className="metric-box"><span>Evidence Type</span><b style={{fontSize:12}}>{e.type}</b></div><div className="metric-box"><span>Year</span><b>2024</b></div></div><h3 style={{font:'600 15px Manrope'}}>Evidence summary</h3><p style={{fontSize:11,color:'var(--muted)',lineHeight:1.7}}>Synthetic prototype evidence record showing how a land-use observation can be connected to a source, verification event and spatial context. It is not an official government record.</p></div><DecisionTrace/></div><div className="grid"><div className="card panel"><div className="section-title"><FileCheck2 size={15}/> Provenance</div><p className="section-sub">Source: {e.source}</p><p className="hash">SHA-256: {e.hash}</p><p style={{fontSize:9,color:'var(--muted)'}}>Timestamp: 28 Apr 2025, 14:25<br/>Verifier: Prototype verification service</p><span className="status verified"><ShieldCheck size={12}/> VERIFIED</span></div><div className="card panel"><div className="section-title">Related evidence</div>{evidence.slice(0,4).map(x=><Link key={x.id} to={'/evidence/'+x.id} style={{display:'block',padding:'9px 0',borderBottom:'1px solid rgba(29,58,85,.5)',fontSize:9,color:'#a9c8df'}}>{x.title}</Link>)}</div></div></div></>}
