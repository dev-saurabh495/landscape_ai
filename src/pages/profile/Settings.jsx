import React from'react';import PageHeader from'../../components/common/PageHeader';import{useApp}from'../../app/providers';const themes=[['midnight','Midnight Blue'],['light','Light Government']];export default function Settings(){const{theme,setTheme,presentation,setPresentation,resetDemo}=useApp();return <><PageHeader eyebrow="PREFERENCES" title="Settings" description="Appearance, notification, language, map and privacy preferences."/><div className="settings-grid"><div className="settings-nav card panel"><button className="active">Appearance</button><button>Notifications</button><button>Language</button><button>Map preferences</button><button>Privacy</button></div><div className="card panel"><div className="section-title">Appearance</div><p className="section-sub">Theme selection persists in localStorage.</p><div style={{display:'flex',gap:8,margin:'14px 0'}}>{themes.map(([id,label])=><button key={id} className={'btn '+(theme===id?'primary':'')} onClick={()=>setTheme(id)}>{label}</button>)}</div><div style={{padding:'12px 0',borderTop:'1px solid var(--border)'}}><label className="check" style={{fontSize:10}}><input type="checkbox" checked={presentation} onChange={e=>setPresentation(e.target.checked)}/> Presentation Mode</label><p className="section-sub">Keeps the demo flow focused on Lucknow and key workflow actions.</p></div><div style={{paddingTop:12,borderTop:'1px solid var(--border)'}}><button className="btn" onClick={resetDemo}>Reset all demo data</button></div></div></div></>}
