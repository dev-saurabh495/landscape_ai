import React from'react';import{ArrowLeft,Download}from'lucide-react';import{Link,useParams}from'react-router-dom';import{reports}from'../../data/mockData';import PageHeader from'../../components/common/PageHeader';import DecisionTrace from'../../components/common/DecisionTrace';export default function ReportDetail(){const{id}=useParams();const r=reports.find(x=>x.id===id)||reports[0];return <><PageHeader eyebrow="INTELLIGENCE REPORT" title={r.title} description={`${r.author} • ${r.date}`} actions={<><Link className="btn" to="/reports"><ArrowLeft size={14}/> Back</Link><button className="btn primary" onClick={()=>alert('PDF export prepared.') }><Download size={14}/> Export PDF</button></>}/><div className="card panel" style={{maxWidth:980,margin:'0 auto'}}><span className="status verified">VERIFIED SOURCES • PROTOTYPE</span><h2 style={{font:'800 26px Manrope'}}>Executive Summary</h2><p style={{fontSize:11,color:'var(--muted)',lineHeight:1.8}}>This synthetic report demonstrates how a district intelligence brief can combine geospatial observations, evidence records, policy scenario estimates and verification status in one auditable narrative.</p>{['Key Findings','Geospatial Analysis','Evidence','Policy Impact','Risk Assessment','Recommendations','Sources','Verification'].map((s,i)=><section key={s} style={{padding:'14px 0',borderTop:'1px solid var(--border)'}}><h3 style={{font:'600 13px Manrope'}}>{i+1}. {s}</h3><p style={{fontSize:10,color:'var(--muted)',lineHeight:1.7}}>Prototype report content for {s.toLowerCase()}. All figures are synthetic demo values and should not be interpreted as official government findings.</p></section>)}</div><div style={{marginTop:12}}><DecisionTrace/></div></>}
