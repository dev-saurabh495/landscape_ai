import React from'react';export default function DataTable({columns,rows,onRow}){return <div className="table-card card"><div className="table-toolbar"><span className="section-title">Records</span><span style={{fontSize:9,color:'var(--muted)',marginLeft:'auto'}}>{rows.length} results</span></div><table className="data-table"><thead><tr>{columns.map(c=><th key={c.key}>{c.label}</th>)}</tr></thead><tbody>{rows.map((r,i)=><tr key={r.id||i} onClick={()=>onRow?.(r)} style={{cursor:onRow?'pointer':'default'}}>{columns.map(c=><td key={c.key}>{c.render?c.render(r):r[c.key]}</td>)}</tr>)}</tbody></table></div>}
